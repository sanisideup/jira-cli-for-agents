type: Task
fields:
  project:
    key: "{{ .Project }}"
  summary: "[Charter] {{ .Summary }}"
  description: |
    ## Problem Statement
    {{ .Problem }}

    ## Goals
    {{ .Goals }}

    ## Success Criteria
    {{ .SuccessCriteria }}
  labels: "{{ .Labels | toJson }}"
